services:
  mqtt:
    image: eclipse-mosquitto
    container_name: mqtt
    expose:
      - 1883
    ports:
      - 1883:1883
    # restart: unless-stopped
    restart: no
    volumes:
      - ./mosquitto/config:/mosquitto/config:ro
